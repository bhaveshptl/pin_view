import 'package:flutter/material.dart';

class EarnCash extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Earn cash"),
      ),
      body: Center(
        child: Text("Earn Cash"),
      ),
    );
  }
}
